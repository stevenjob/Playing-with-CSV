'use strict';

describe('csvApp.import module', function() {

  beforeEach(module('csvApp.import'));

  describe('import controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('ImportCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});