'use strict';

describe('csvApp.list module', function() {

  beforeEach(module('csvApp.list'));

  describe('list controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ListCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});